---
scope: oauth process telemetry only
purpose: build-substrate for the oauth Prometheus pull exporter
altitude: feature
updated: 2026-09-21T18:01
---

- (direction) Fast path. Approved plan 2026-09-21 adopts the lean: Prometheus pull on the existing metrics port; OTLP is not the success signal.
- (note) Input brief: _bmad-output/planning-artifacts/brief-grupo07-oauth-observability.md. Brownfield: NestJS 11 on Node 22, main.ts listens 0.0.0.0 on OAUTH_INTERNAL_API_PORT only.
- (version) Verified 2026-09-21 on npm: @opentelemetry/api 1.9.1, @opentelemetry/sdk-node 0.221.0, @opentelemetry/exporter-prometheus 0.221.0, @opentelemetry/instrumentation-http 0.221.0. exporter-prometheus and sdk-node must be >= 0.217.0 because CVE-2026-44902 crashes the process on a malformed metrics request below that.
- (version) sdk-node 0.222.0 exists (2026-08-31) but exporter-prometheus and instrumentation-http verified line is 0.221.0. Pin the 0.221.0 set so the three experimental packages match.
- (decision) AD-1 [ADOPTED] Paradigm is in-process OpenTelemetry SDK with a Prometheus pull exporter. Binds all oauth telemetry. Prevents a push-only OTLP path and a hand-rolled prom-client. Rule: metrics leave the process only when Prometheus scrapes /metrics.
- (decision) AD-2 [ADOPTED] Metrics server binds 0.0.0.0 on OAUTH_INTERNAL_METRICS_PORT (professor .env value 9464) at path /metrics. Binds the metrics listener. Prevents binding 127.0.0.1 or serving metrics on the API port. Rule: do not invent a new env name; read OAUTH_INTERNAL_METRICS_PORT.
- (decision) AD-3 [ADOPTED] Start the SDK before NestFactory.create so HTTP instrumentation patches the server that then listens on OAUTH_INTERNAL_API_PORT. Binds bootstrap. Prevents registering instrumentation after listen. Rule: one SDK start per process; no second MeterProvider.
- (decision) AD-4 [ADOPTED] Instrument HTTP only, via @opentelemetry/instrumentation-http. Binds metric production. Prevents the auto-instrumentations metapackage and any trace exporter. Rule: NodeSDK metricReader is PrometheusExporter; do not set an OTLP trace or log exporter.
- (decision) AD-5 [ADOPTED] Retarget professor prometheus.yml only: job auth target becomes oauth:9464 path /metrics, and the blackbox URL http://auth:3001/health becomes http://oauth:3001/health. Binds the scrape contract. Prevents leaving the dead hostname auth or editing other jobs, compose, or .env. Rule: no other lines in prometheus.yml change.
- (decision) AD-6 [ADOPTED] Metric labels and values must not contain access tokens, refresh tokens, passwords, client secrets, or the Authorization header. Binds every metric attribute. Prevents putting credential material on http.target or custom attributes. Rule: do not add those fields as attributes; do not log the metrics payload with secrets.
- (decision) AD-7 [ADOPTED] T1 HTTP contracts stay. Binds login, users, roles, authz, OA error body, and GET /health. Prevents changing status codes or bodies to suit metrics. Rule: GET /health still returns status ok and the compose healthcheck still hits the API port.
- (constraint) Telemetry code lives in submodule backend/oauth. The prometheus.yml retarget lives in the parent repo. Compose and .env are not invented.
- (assumption) HTTP exposition after one API call contains an http_server_request_duration or http_server_duration series. instrumentation-http 0.221.0 is on the stable HTTP semantic-convention line; the kernel tests for that family rather than one exact name.
- (decision) Deferred: OTLP push to otel-collector, trace and log storage, Grafana, custom business counters beyond HTTP instrumentation, histogram bucket overrides, other course services.
- (event) Spine distilled from this memlog into ARCHITECTURE-SPINE.md
- (decision) Reviewer gate: AD-5 now renames job_name to oauth and retargets oauth:9464, and changes only the auth health URL in the blackbox job. Prevents a job still named auth.
- (decision) Reviewer gate: AD-2 now says the Prometheus exporter is the only listener on the metrics port. Prevents a second Nest server on 9464.
- (decision) Reviewer gate: AD-1 now sets resource service.name to oauth. Prevents a resource labeled auth.
- (event) lint_spine.py ok with zero findings after the gate edits
- (event) spine finalized
