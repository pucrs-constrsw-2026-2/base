---
topic: Grupo 07 oauth Prometheus metrics
updated: 2026-09-21T18:02
---

- (note) Express distill. Inputs: brief-grupo07-oauth-observability.md and the finalized spine architecture-constrsw-2026-2-2026-09-21. Approved plan closed the forks; no guided elicitation.
- (direction) Sibling spec slug grupo07-oauth-observability. Do not fold into spec-grupo07-keycloak-oauth. T1 capability IDs stay stable.
- (decision) AD-1 adopted: Prometheus pull only. Metrics leave the process when scraped at /metrics. No OTLP exporter. service.name is oauth.
- (decision) AD-2 adopted: bind 0.0.0.0 to OAUTH_INTERNAL_METRICS_PORT, path /metrics. Prometheus exporter is the only listener on that port.
- (decision) AD-3 adopted: one SDK start before NestFactory.create.
- (decision) AD-4 adopted: instrumentation-http only. No auto-instrumentations metapackage.
- (decision) AD-5 adopted: prometheus.yml job_name oauth, target oauth:9464, path /metrics; blackbox URL http://oauth:3001/health. No other professor file edits.
- (decision) AD-6 adopted: tokens, passwords, client secret, and Authorization header value stay out of metric labels and samples.
- (decision) AD-7 adopted: T1 login and GET /health contracts stay.
- (constraint) Use professor env names OAUTH_INTERNAL_METRICS_PORT and OAUTH_INTERNAL_API_PORT. Do not invent compose or .env keys.
- (constraint) exporter-prometheus and sdk-node at 0.217.0 or newer because CVE-2026-44902. Pins live in the adopted spine.
- (constraint) Telemetry code in submodule backend/oauth. Scrape edit in the parent prometheus.yml.
- (capability) CAP-1 The oauth process exposes Prometheus text on its metrics port. Success: GET /metrics on the metrics port returns 200 and Prometheus text.
- (capability) CAP-2 An HTTP call to the oauth API increases an HTTP server metric. Success: after GET /health, /metrics contains a series starting with http_server_request_duration or http_server_duration.
- (capability) CAP-3 Metric labels and samples do not carry credentials. Success: after a login attempt, /metrics does not contain the password, access token, client secret, or Authorization header value.
- (capability) CAP-4 Login and health keep the T1 HTTP contracts. Success: GET /health still returns status ok, and successful POST /login still returns 200 with the T1 token fields.
- (assumption) HTTP series name is in the http_server_request_duration or http_server_duration family. CAP-2 accepts either prefix.
- (assumption) Professor .env keeps OAUTH_INTERNAL_METRICS_PORT=9464 and OAUTH_INTERNAL_API_PORT=3001.
- (note) Non-goals: Grafana, alert and recording-rule edits, OTLP traces and logs, collector config changes, other course services, business counters beyond HTTP, rewriting the OA API.
- (note) Wrapper-only dropped: Mary process notes about what the brief itself would not choose. The closed forks are the decisions above.
- (event) Pass 1 Coherence: PASS. CAP-1 through CAP-4 each have intent and success. Constraints bend design. Non-goals present. Success signal is demonstrable. IDs stable. Intents stay what, not how.
- (event) Pass 2 Preservation: PASS. Pull-versus-push, scrape hostname, traces and logs out of scope, redaction, port, bootstrap, HTTP-only, CVE floor, T1 contracts, and the external volume all land in SPEC.md, scrape-contract.md, architecture-diagrams.md, or the adopted spine.
- (event) spec finalized
