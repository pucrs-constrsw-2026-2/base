---
topic: Grupo 07 Keycloak OAuth API (T1 + authz)
updated: 2026-09-03T18:31
---

- (direction) Create-only SPEC slug grupo07-keycloak-oauth; scope Keycloak + oauth API (T1 A + Keycloak B); ignore/defer professors/Postgres C and architecture spine
- (direction) Professor provides .env and docker-compose.yml — do not invent compose as deliverable; plug oauth service/Dockerfile into whatever they give
- (note) Source: _bmad-output/planning-artifacts/requirements-grupo07-t1-oauth-professors.md sections A+B; D conflicts for oauth/Keycloak only
- (decision) D.1 Login request body from client is form-data username+password only; API injects client_id, client_secret, grant_type=password from env (T1 wins over variant listing client_id/grant_type in body)
- (decision) D.2 Login success HTTP status is 201 per T1 (not Keycloak native 200)
- (decision) D.3 Canonical client roles for authz matrix are administrator, coordinator, professor, student (B.2). Local realm-export.json funcionario/coordenador is non-authoritative draft; verify professor-imported realm matches B.2 or align names before coding policies
- (decision) D.4 Prefer Keycloak 26 URL style without /auth path prefix (e.g. /realms/{realm}/...); verify against professor compose base URL — if /auth is present, use that base
- (decision) D.5 Authorization resources/policies/permissions + oauth validate endpoint are in MVP scope for this SPEC (Section B active)
- (note) D.6 Professors main/secondary entity pair deferred — out of scope with section C
- (constraint) Work on group branch; implement in backend/oauth submodule + Dockerfile; realm constrsw, client oauth
- (constraint) Preserve brief typo referesh_expires_in in login response field name
- (constraint) Soft-delete users/roles = disable / logical delete in Keycloak, not hard remove
- (constraint) Email-as-username; validate email with RFC 5322 regex from brief on POST /users
- (capability) CAP-1 Login: obtain Keycloak tokens via password grant; return token_type, access_token, expires_in, refresh_token, referesh_expires_in; codes 201/400/401
- (capability) CAP-2 Users CRUD: create/list/get/update/password-patch/soft-delete against Keycloak Admin API with Bearer auth and enabled filter
- (capability) CAP-3 Roles CRUD plus assign/unassign role to user (logical delete on DELETE)
- (capability) CAP-4 Uniform oauth error body OA-000 shape with error_code, error_description, error_source, error_stack; relay Keycloak codes unless specified
- (capability) CAP-5 Keycloak authz config in constrsw/oauth: four roles, eight resources with URLs, role policies, resource-based permissions per B.2 matrix
- (capability) CAP-6 Authz validate endpoint: given access token + requested resource, return 200 if role grants access else 403
- (capability) CAP-7 Token refresh: exchange refresh_token for new tokens following Keycloak OIDC refresh_token grant (dedicated route)
- (capability) CAP-8 Package oauth API with Dockerfile and wire into professor-provided compose/.env when oauth service is uncommented/enabled
- (assumption) Professor compose uses quay.io/keycloak/keycloak ~26.x with realm import for constrsw/oauth; ignore historical jboss/keycloak run command except as reference
- (assumption) Keycloak base URL for API calls omits /auth unless professor .env/compose proves otherwise
- (assumption) client_secret and Keycloak admin credentials for Admin API live in professor-provided .env
- (assumption) UserInfo and token endpoints are reference for integration/testing; login path is the primary T1 user-facing token acquisition
- (question) Exact path and request contract for CAP-6 validate endpoint (path/query/body fields) not specified in brief — propose /authz/validate with resource identifier
- (question) Exact path for CAP-7 refresh route not in brief — propose POST /refresh with refresh_token form field
- (question) Whether roles are realm roles or client roles of oauth — B.2 says filter by client when picking role → treat as client roles on oauth
- (event) Express distill chosen: input structured requirements + explicit orientation; no guided elicitation
- (decision) Roles for authz are client roles on client oauth (B.2: filter by client when picking role)
- (decision) CAP-6 default contract: POST /authz/validate with Authorization Bearer + JSON { resource } matching one of the eight resource names; 200 OK / 403 Forbidden / 401 missing-invalid token — provisional until professor specifies otherwise
- (decision) CAP-7 default contract: POST /refresh form-data refresh_token; server adds client_id/client_secret/grant_type=refresh_token; response mirrors login token fields; codes 200 or 201 TBD prefer 200 for refresh vs 201 for login — use 200 for refresh
- (assumption) Resource URLs for the eight authorization resources are set in Keycloak to identifiable paths (e.g. /professors); exact URL strings may follow professor template if provided
- (note) Wrapper-only dropped: historical jboss/keycloak docker run (B.1) kept only as superseded reference note in companion; full course mesh; custom BMAD agents; Prometheus/Mongo; Astah deliverables
- (event) Pass 1 Coherence: PASS — CAP-1..8 each have intent+success; constraints bend design; non-goals present; success signal demonstrable; IDs stable; lean enough
- (event) Pass 2 Preservation: PASS — A.1-A.3 routes/errors, B.2-B.4 authz/OIDC/refresh, D.1-D.5 resolutions, professor compose/.env plug-in, KC26 /auth assumption all in SPEC or companions; C professors/Postgres deferred as non-goal; B.1 jboss run wrapper-only superseded note
- (event) Spec create complete: SPEC.md + oauth-api.md + keycloak-authz.md at _bmad-output/specs/spec-grupo07-keycloak-oauth/
- (note) Companions: oauth-api.md (routes/errors/refresh), keycloak-authz.md (roles/resources/policies/validate); no architecture-diagrams by orientation
- (decision) Open Q1 resolved: professor/B.2 role names match (administrator/coordinator/professor/student); user will notify if future import diverges
- (decision) Open Q2 resolved: CAP-6 MUST call Keycloak Authorization Services (not local static matrix evaluation); static matrix remains the expected outcome oracle
- (direction) Future elicitation: when presenting options, mark preferred choice with *recommended tag
- (event) SPEC update: cleared Open Questions; CAP-6/keycloak-authz tightened to Keycloak Authorization Services
- (event) 2026-09-03 Professor stack merged from main: compose+.env+constrsw.json present; oauth service already enabled; CAP-8/1.3 reframed to verify; CAP-5/6.1-6.4 verify-first; .env KEYCLOAK_SERVER_URL literal http://keycloak:8080; OAUTH_INTERNAL_HOST=oauth
